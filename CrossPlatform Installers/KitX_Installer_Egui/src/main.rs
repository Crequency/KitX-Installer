#![allow(non_snake_case)]

fn main() {
    println!("Hello, world!");

    let native_options: NativeOptions = eframe::NativeOptions { ..Default::default() };
    // eframe::run_native(app_name: "KitX Installer", native_options, app_creator);
}
